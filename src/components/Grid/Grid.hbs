<div class="content__grid"

     style="grid-template-columns: repeat({{{column}}}, 1fr);
             grid-column-gap: {{{gap}}}; grid-row-gap: 10px">
    {{#each content}}
        {{{this}}}
    {{/each}}
</div>
